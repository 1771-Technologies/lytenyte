---
title: Export Parquet
step: Export data from LyteNyte Grid as a Parquet file.
description: Apache Parquet is an open source, column-oriented data file format. Using
  LyteNyte Grid's data export functionality, you can create and download Parquet files.
---

This guide uses the
[Hyparquet Writer](https://github.com/hyparam/hyparquet-writer) library to create Parquet
files in the browser. Install this package to enable exporting data to Parquet files.

<PackageInstall package="hyparquet-writer" />

## Download a Parquet File

The demo below demonstrates exporting data from the grid and creating and downloading
a Parquet file. Because Parquet files are binary, you will need a
[Parquet file viewer](https://hyperparam.app/)
to inspect the output.

::demo[Export Parquet File="./demos/parquet-export"]

The code below shows how to create and download a Parquet file. Because the Parquet
format is columnar, the code first constructs an object of arrays, where each array
represents a column. The code then passes this object to `parquetWriteBuffer`, which
returns an `ArrayBuffer`. The code converts this buffer into a file and downloads it.

```tsx
<button
  data-ln-button="tertiary"
  data-ln-size="md"
  //!next 19
  onClick={async () => {
    const api = apiRef.current;
    if (!api) return;

    const rect = await api.exportData();

    const columns = Object.fromEntries(
      rect.columns.map((x) => [
        x.id,
        {
          name: x.id,
          data: [] as any[],
        } satisfies ColumnSource,
      ]),
    );

    for (let i = 0; i < rect.data.length; i++) {
      const data = rect.data[i];

      for (let j = 0; j < rect.columns.length; j++) {
        const column = rect.columns[j];
        columns[column.id].data.push(data[j]);
      }
    }

    const parquetBuffer = parquetWriteBuffer({
      columnData: Object.values(columns),
    });

    downloadBlob(new Blob([parquetBuffer], { type: "application/octet-stream" }), "data.parquet");
  }}
>
  Download Parquet File
</button>
```

## Next Steps

::next[/docs/export-csv]
::next[/docs/export-arrow]
::next[/docs/export-excel]
