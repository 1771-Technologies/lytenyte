---
import { Steps as SLSteps } from "@astrojs/starlight/components";
---

<div class="ln-steps my-8">
  <SLSteps>
    <slot />
  </SLSteps>
</div>

<style>
  .ln-steps {
    --sl-line-height: 1.75;
    --sl-steps-start: 0;
    --sl-color-hairline-light: var(--color-ln-border);

    .sl-steps {
      margin: 0px;
    }

    .sl-steps li {
      padding-top: 0px;
    }
    .sl-steps li:not(:last-child) {
      padding-bottom: 24px;
    }

    .sl-steps > li::before {
      display: flex;
      justify-content: center;
      align-items: center;
      font-size: 12px;
      color: var(--color-ln-primary);
      background-color: var(--color-ln-card);
      border: 1px solid var(--color-ln-border);
    }
  }
</style>
